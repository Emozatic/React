//Passing arrays to Props   
    we can pass array in the props
    =>we can use array with an external variable or we can use without variable
    =>we can pass object and access through with their key.

    =>we can see that the array elements are not show seperately, they are stick we each others because of babble conversion

    =>So, we can use html <li> type tags to write the elements seperately.
                            OR 
    =>We can use map funciton to return the arrays elements.